# devforge run data. Runs write their own .devforge/.gitignore that keeps only
# config.json and registry.json; run evidence is summarized in the PR body instead.
# This repo does not commit any run data; shipped defaults live beside the skill.
.devforge/

# Editor and OS noise
.idea/
.DS_Store

# Python artifacts from local tests
__pycache__/
*.pyc
.pytest_cache/
